'use strict';

/**
 * sub-additional-cat router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sub-additional-cat.sub-additional-cat');
