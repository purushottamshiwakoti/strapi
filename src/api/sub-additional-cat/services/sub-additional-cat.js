'use strict';

/**
 * sub-additional-cat service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sub-additional-cat.sub-additional-cat');
