'use strict';

/**
 * additional-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::additional-category.additional-category');
