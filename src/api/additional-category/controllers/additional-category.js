'use strict';

/**
 * additional-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::additional-category.additional-category');
